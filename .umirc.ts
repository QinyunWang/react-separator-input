import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'react-separator-input',
  favicon: '/assets/ke.ico',
  logo: '/images/logo.svg',
  outputPath: 'docs-dist',
  // more config: https://d.umijs.org/config
});
